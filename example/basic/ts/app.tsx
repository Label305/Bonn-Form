import * as React from 'react';
import * as ReactDOM from 'react-dom';
import {TheForm} from './components/the_form';

const element = document.getElementById('container');

ReactDOM.render(<TheForm/>, element);